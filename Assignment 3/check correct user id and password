# check if user has enterd correct user id and password.

user_id = str(input("Enter user_id:"))
passowrd = int(input("Enter password:"))

user_id =  "Fbs"
password = 8080

if(user_id == "Fbs"  and  password == 8080):
    print("login successfully!")
else:
    print("incorrect userid or password")
        
    